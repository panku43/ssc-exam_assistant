git init  # Initializes a Git repository
git add .  # Adds all files to the staging area
git commit -m "Initial commit"  # Commits the files
git remote add origin https://github.com/your-username/ssc-exam-assistant.git  # Replace with your repo URL
git push -u origin master  # Pushes the code to GitHub
